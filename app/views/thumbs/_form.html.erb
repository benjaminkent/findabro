<%= form_with(model: thumb, local: true) do |form| %>
  <% if thumb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thumb.errors.count, "error") %> prohibited this thumb from being saved:</h2>

      <ul>
      <% thumb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
